package modelo;

public class Accesorio extends Producto{
    public Accesorio(String nombre, double precio, String categoria, String descripcion, String plataforma) {
        super(nombre, precio, categoria, descripcion, plataforma);
    }
}
